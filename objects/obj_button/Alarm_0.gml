fake_buttons = 0
if (obj_player.sanity<=90 and irandom_range(0,1)=1) fake_buttons++
if (obj_player.sanity<=70 and irandom_range(0,1)=1) fake_buttons++
if (obj_player.sanity<=50 and irandom_range(0,1)=1) fake_buttons++
if (obj_player.sanity<=30 and irandom_range(0,1)=1) fake_buttons++
if (obj_player.sanity<=10 and irandom_range(0,1)=1) fake_buttons++
alarm[0] = room_speed*3